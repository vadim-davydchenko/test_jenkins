pipeline {
  agent any
  stages {
    stage('Build') { 
      steps { 
        echo 'Build from DEV branch'
      } 
    }
    stage('Unit tests') { 
      steps { 
        echo 'Unit tests from DEV branch' 
      } 
    }
  }
}
